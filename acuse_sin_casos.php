<?php require_once __DIR__.'/lib/inicio.php';exigir_sesion();require_once __DIR__.'/lib/PdfSimple.php';$c=obtener_caso((int)$_GET['id']);$p=new PdfSimple;$p->titulo('ACUSE DE REGISTRO SIN CASOS DE ACTUALIZACIÓN');$p->linea('Folio: '.$c['folio'],13,true);$p->linea('Dirección Distrital: '.$c['numero_distrito']);$p->linea('Fecha de registro: '.$c['fecha_registro']);$p->linea();$p->linea('DECLARACIÓN',12,true);$p->linea('La Dirección Distrital informa que no presenta casos de actualización del Marco Geográfico de Participación Ciudadana para el periodo registrado.');$p->linea();$p->linea('Este acuse no requiere firma autógrafa y se valida mediante el folio institucional.');$p->descargar($c['folio'].'_acuse_sin_casos.pdf');