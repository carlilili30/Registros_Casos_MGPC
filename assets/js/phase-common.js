import {API} from './api.js';import {CONFIG} from './config.js';import {qs,notify,initShell,getCaseId,formObject,localDateTime,esc} from './common.js';
initShell();const phase=Number(document.body.dataset.phase),id=getCaseId();const labels=['Registro','Sistema SAM','Encuestas','Portal SAM','Integración','Expediente','Revisión CG','Cédula'];
async function load(){if(!id){notify('Selecciona un caso para continuar.','error');qs('#phaseForm button[type=submit]').disabled=true;return}const c=await API.record(CONFIG.tables.cases,id);qs('#caseInfo').innerHTML=`Caso: <strong>${esc(c.folio||id)}</strong>`;qs('#timeline').innerHTML=labels.map((x,i)=>`<div class="step ${i+1<Number(c.fase_actual)?'done':i+1===Number(c.fase_actual)?'active':''} ${c.clasificacion===CONFIG.classificationSkip&&i+1===4?'skipped':''}">${i+1}. ${x}</div>`).join('')}
qs('#phaseForm')?.addEventListener('submit',async e=>{e.preventDefault();const btn=e.currentTarget.querySelector('button[type=submit]');btn.disabled=true;try{const data=formObject(e.currentTarget);delete data.archivos;await API.create(CONFIG.tables.phases,{id_caso:Number(id),fase:phase,estatus:'CONCLUIDA',observaciones:data.observaciones||'',datos_json:JSON.stringify(data),fecha_fin:localDateTime()});const files=qs('#archivos')?.files;if(files?.length){const fd=new FormData();[...files].forEach(f=>fd.append('files[]',f));fd.append('descripcion',`Caso ${id}, fase ${phase}`);const up=await API.upload(CONFIG.tables.files,fd);for(const f of up.subidos||[])await API.create(CONFIG.tables.caseFiles,{id_caso:Number(id),id_archivo:Number(f.id),fase:phase,nombre_original:f.nombre_original})}const c=await API.record(CONFIG.tables.cases,id);if(phase===8){await API.update(CONFIG.tables.cases,id,{fase_actual:8,estatus:'CONCLUIDO'});location.href=`../casos/detalle.html?id=${id}`;return}let next=phase+1;if(phase===3&&c.clasificacion===CONFIG.classificationSkip)next=5;await API.update(CONFIG.tables.cases,id,{fase_actual:next});const slugs=['','registro','sistema-sam','encuestas','portal-sam','integracion-propuesta','conformacion-expediente','revision-cg','cedula-notificacion'];location.href=`fase${next}-${slugs[next]}.html?id=${id}`}catch(x){notify(x.message,'error');btn.disabled=false}});load();
